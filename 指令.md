# 1. 进入backend目录
cd backend

# 2. 创建虚拟环境
python -m venv venv

# 3. 激活虚拟环境
# Windows:
venv\Scripts\activate

# 4. 验证虚拟环境激活
# 命令行前面应该显示 (venv)


# 1. 安装核心依赖
pip install django
pip install djangorestframework
pip install django-cors-headers

# 2. 安装数据库驱动（MySQL）
pip install mysqlclient

# 3. 安装其他实用工具
pip install pillow  # 图片处理
pip install bcrypt  # 密码加密
pip install python-dotenv  # 环境变量

# 4. 创建requirements.txt文件
pip freeze > requirements.txt


# 1. 创建Django项目
django-admin startproject lostfound .

# 2. 创建应用（按照需求模块划分）
python manage.py startapp users
python manage.py startapp items
python manage.py startapp notices
python manage.py startapp operations
python manage.py startapp config

# 3. 查看项目结构
# 此时backend目录结构应为：
# backend/
# ├── lostfound/      # 项目配置
# ├── users/          # 用户管理
# ├── items/          # 物品管理
# ├── notices/        # 公告通知
# ├── operations/     # 操作日志
# ├── config/         # 系统配置
# ├── venv/          # 虚拟环境
# ├── manage.py      # 管理脚本
# └── requirements.txt


# 1. 返回项目根目录
cd ../frontend

# 2. 使用Vite创建Vue项目（更轻量快速）
npm create vue@latest .

# 3. 安装过程中选择：
#   √ 添加TypeScript? Yes
#   √ 添加JSX支持? No
#   √ 添加Vue Router? Yes
#   √ 添加Pinia? Yes（状态管理）
#   √ 添加Element Plus? Yes（UI组件库）
#   √ 添加ESLint? Yes
#   √ 添加Prettier? Yes

# 4. 安装依赖
npm install

# 5. 安装Axios（HTTP请求）
npm install axios

# 6. 安装其他工具
npm install dayjs  # 时间处理
npm install lodash-es  # 工具库